<%= nested_form_for @faculty_propsel, url: url, remote: form_request, html: { multipart: true }  do |f| %>
    <% if @faculty_propsel.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@faculty_propsel.errors.count, "error") %> prohibited this nomination from being saved:</h2>

        <ul>
        <% @faculty_propsel.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <h3>Faculty Info:</h3>
    
    <div class="field">
      <div class="form-group">
        <label>*Name</label>
        <%= f.text_field :name,class: "form-control form-control" %>
      </div>
    </div> 

    <div class="field">
      <label>*Address</label> 
      <%= f.text_field :address,class: "form-control" %>
    </div> 
    <div class="field">
      <label>*City</label> 
      <%= f.text_field :city,class: "form-control" %>
    </div>
    <div class="field">
      <label>*State</label>  
      <%= f.text_field :state,class: "form-control" %>
    </div>
    <div class="field">
      <label>*Zip</label>
      <%= f.text_field :zip,class: "form-control" %>
    </div>
    <div class="field">
      <label>*Office Phone</label>
      <%= f.text_field :office_phone ,class: "form-control"%>
    </div>
    <div class="field">
      <label>Mobile Phone</label>
      <%= f.text_field :mobile_phone,class: "form-control" %></div>
    <div class="field">
      <label>*Area of Expertise</label>
      <%= f.text_field :area_of_expertise, class: "string required form-control datepicker", required: true %>
    </div>
    <div class="field">
      <label>*Geographic/day/evening preferences or limitations</label>
      <%= f.text_field :limitations,class: "form-control" %>
    </div>
    <hr>

    <h3>If you have a continuing education activity/program idea, please tell us about it below.</h3>

    <div class="field">
      <lable>*Program Title</lable>   
      <%= f.text_field :program_title,class: "form-control" %>
    </div>
    <div class="field">
      <lable>*Topic </lable>   
      <%= f.text_field :area,class: "form-control"  %>
    </div>

    <div class="field">
      <lable>*Program Length (1 hr, 1.5 hr, 2 hr, half day, full day)</lable>   
      <%= f.select :program_length, options_for_select(["1 hr", "1.5 hr", "2 hr", "hr, half day","full day"]),{},  class: "form-control" %>
    </div>
    <div class="field">
      <lable>*Will the activity be knowledge-based (K), application-based (A), or a practice-based(P)? </lable>   
      <%= f.select :activity_based, options_for_select(["knowledge-based (K)", "application-based (A)","practice-based(P)"]),{},class: "form-control" %>
    </div>
    <div class="field">
     <lable> *Brief summary of the program</lable> 
      <%= f.text_field :brief_summary,class: "form-control" %>
    </div>
    <h3> Please provide your CV with the completed form. </h3>
    <div class="field">
      As a NJPhA member benefit, each issue of the New Jersey Journal of Pharmacy has a CE article posted to our website (free for members).  Would you be interested in volunteering to author a home study CE or know an interested colleague?
      <%= f.text_field :intrested_memeber,class: "form-control" %> 
    </div>
    <div class="field">
    <%= f.fields_for :proposel_documents do |f| %>
        <%= f.file_field :document ,:multipart=>true %>
        <%= f.link_to_remove "Remove Document" %>
      <% end %>
    </div>
    <%= f.link_to_add "Upload Document", :proposel_documents %>

    <div class="actions">
      <%= f.button 'Submit', class: 'btn btn-primary' %>
    </div>
<% end %>